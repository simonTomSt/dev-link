import { AsyncPayload } from "./asyncModels";
import { Dispatch } from "redux";

// export const setAlert = ({ alertMsg, alertType }: AsyncPayload) => (
//   dispatch: Dispatch
// ) => {
//   dispatch({
//     type: alertTypes.SET_ALERT,
//     payload: { alertMsg, alertType },
//   });
// };
